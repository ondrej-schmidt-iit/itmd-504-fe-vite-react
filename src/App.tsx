import React from 'react';
import Directory from './components/directory';

const App: React.FC = () => {
    return (
        <div>
            <Directory />
        </div>
    );
};

export default App;